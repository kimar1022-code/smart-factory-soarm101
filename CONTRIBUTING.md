# 기여 가이드

이 저장소에서 작업할 때의 규칙이다.
혼자 작업하더라도 규칙을 적어 두면 몇 주 뒤의 나에게 도움이 된다.

## 브랜치

| 브랜치 | 용도 |
|---|---|
| `main` | 동작이 검증된 상태. 실물 로봇으로 한 번은 돌려 본 것만 올린다 |
| `local-full` | 작업 브랜치. `main` 과 같은 지점을 가리킨다 |
| `feat/<이름>` | 기능 추가 |
| `fix/<이름>` | 버그 수정 |
| `docs/<이름>` | 문서만 바꿀 때 |

예: `feat/waypoint-blend`, `fix/gripper-settle`

`v0.2-archive` 태그는 구조가 완전히 다른 옛 버전을 보존한 것이다.
`main` 과 공통 조상이 없으므로 머지하지 말 것.

## 커밋

제목은 한 줄로 무엇이 바뀌었는지 적는다. 마침표는 붙이지 않는다.

```
재생: 한 동작이 끝난 뒤에 다음 동작이 시작하도록
```

본문에는 **왜** 그렇게 했는지를 적는다. 무엇을 바꿨는지는 diff 를 보면 알 수 있지만,
왜 그 방법을 골랐고 다른 방법은 왜 안 됐는지는 코드에 남지 않는다.

이 저장소의 버그는 대부분 "로그는 성공인데 실제로는 아무 일도 안 일어남" 부류였다.
그래서 커밋 본문에 다음을 남긴다.

- 증상 (사람이 본 것)
- 원인 (레지스터·타이밍 수준까지)
- 왜 그 해법인지, 검토했다가 버린 대안

## 실물 로봇을 건드리는 변경

12V 로봇팔이 물리적으로 움직인다. 다음은 매번 확인한다.

1. **토크를 끄는 코드를 추가했는가.** 켜진 채로 종료해야 팔이 안 주저앉는다
2. **목표 위치를 쓰기 전에 현재 위치로 동기화했는가.** 안 하면 기동 순간 팔이 튄다
3. **속도 제한이 걸려 있는가.** 슬라이더를 확 움직여도 급가속하면 안 된다
4. **레지스터 쓰기를 되읽어 확인했는가.** `Max_Velocity` 처럼 존재하지 않는
   레지스터에 써도 `try/except` 가 예외를 삼켜 로그만 성공으로 남는다

실물 검증 없이 `main` 에 올리지 않는다.

## 코드 스타일

**C# (Unity)**

- 파일 하나에 클래스 하나. 파일명과 클래스명을 맞춘다
- `public` 필드는 인스펙터에 노출할 것만. 나머지는 `[SerializeField] private`
- 주석은 "무엇을 하는가" 대신 **"왜 이렇게 하는가"** 를 적는다.
  특히 직관에 어긋나는 코드에는 반드시 근거를 남긴다
- Edit 모드에서 `onClick.AddListener` 로 붙인 람다는 직렬화되지 않는다.
  생성(`BuildUI`)과 배선(`WireUp`)을 분리할 것

**Python (라즈베리파이)**

- 표준 라이브러리와 LeRobot SDK 외 의존성을 늘리지 않는다
- 모터 레지스터 이름은 상수로 두지 말고 그대로 쓴다. 검색이 쉬워야 한다
- 예외를 삼키지 않는다. 삼켜야 한다면 무엇을 삼켰는지 로그로 남긴다

## 커밋하지 않는 것

`.gitignore` 에 규칙이 있다. 요지는 다음과 같다.

- Unity 생성물 (`Library/`, `Temp/`, `Logs/`, `*.csproj`, `*.sln`)
- 로그, 크래시 복구본, 압축 백업, 일회성 스크립트
- 자격증명이 든 파일

한 번 커밋된 것은 히스토리에 남는다. 비밀번호나 SSH 계정이 든 파일은
커밋 전에 반드시 확인할 것.

## 문서

기능을 바꾸면 다음도 같이 고친다.

| 바꾼 것 | 같이 고칠 문서 |
|---|---|
| 통신 프로토콜 | `README.md` 프로토콜 표 |
| 새 기능 | `README.md` 기능 절, `CHANGELOG.md` |
| 설치 절차 | `docs/SETUP.md` |
| 요구사항·아키텍처 | `docs/spec/` |

며칠 헤맨 문제는 `docs/TROUBLESHOOTING_*.md` 에 경위와 재현 방법을 남긴다.
README 의 표는 요약이고, 상세는 그쪽에 둔다.
