# 변경 이력

날짜 기준으로 적는다. 실물 로봇에서 확인한 것만 올린다.

---

## 2026-08-04

### 변경

- **홈 자세를 접힌 자세 `(0, -90, 64, -80, 0)` 로 바꿨다.** 전관절 0° 였던 예전 홈은
  팔을 앞으로 쭉 뻗은 자세(TCP 가 베이스에서 0.45m)라 켤 때마다 책상 앞을 크게 쓸었다.
  손으로 접어 둔 실물 자세를 그대로 쓰려 했으나 `elbow_flex` 가 소프트 리밋
  (로봇1 70° / 로봇2 64°)을, `wrist_flex` 가 하드 리밋(-95°)을 넘어서 불가능했다.
  두 로봇 공통 범위 안에서 가장 접힌 값으로 정했다.
  실물 두 대로 홈 이동을 확인했다. 잔차는 elbow +2°, wrist -4° 로 두 대가 같은 방향·
  같은 크기였다. 로봇마다 다르면 리밋에 잘린 것인데 그렇지 않고, elbow 가 명령값 64°
  보다 위로 간 것도 잘린 게 아니라는 뜻이다. 서보가 부하를 받고 정착한 값이다.
- 구 UI 의 홈 버튼이 서버 `type:"home"`(무조건 전관절 0°)을 쓰고 있었다.
  관제 화면과 홈이 갈리므로 `GoToHome()` 으로 통일했다.

### 추가

- `Assets/Editor/HomePoseSetup.cs` — 홈 값이 컨트롤러 4개(로봇1·2 × 시뮬·실물)에
  흩어져 있어 손으로 고치면 어긋난다. `SO-ARM → 홈 포즈 적용` 메뉴로 한 번에 맞춘다.
  리밋 밖 값이면 조용히 자르지 않고 어느 관절인지 경고한다.

### 문서

- **회전 조그의 실제 동작을 실측해 정리했다.** `Rx` 는 "안 되는 축" 이 아니라
  자세를 심하게 타는 축이다. `Rx` 회전은 J1(베이스 yaw)로 만들어지므로 TCP 가
  베이스 축 위에 있으면 통과하고(홈에서 2.10mm), 팔이 조금만 펴지면 크게 밀려
  거절된다(J2 를 -75° 로만 펴도 47.91mm). 실용적으로는 거의 항상 막힌다.
  `Ry` 도 뻗은 자세에서 한계에 걸린 사례가 있고, `Rz` 만 어느 자세에서나 안정적이다.
- 카티시안 IK 를 진행 상태에서 완료로 옮기고, `ik` / `fk` 프로토콜을 표에 넣었다.
- 예전 문서의 "홈 자세가 리치의 96%" 는 틀린 계산이었다. 0.391 은 TCP 의 X 성분이고
  베이스에서의 실제 거리는 0.452 다.

---

## 2026-08-02

### 추가

- **관제 화면 (Control Tower).** 상태 감시와 제어를 한 화면에 모았다.
  왼쪽에 두 로봇의 관절 슬라이더, 가운데에 TOP / SIDE / FRONT 뷰,
  오른쪽 아래에 온도·전압·부하 상태표. 각 뷰는 드래그로 돌리고 휠로 확대한다.
- **루틴 불러오기 창.** 저장된 루틴을 목록에서 골라서 연다.
  최근 수정순으로 정렬하고, 현재 스텝이 사라진다는 것을 미리 알린다.
- **레이아웃 저장/복원.** 에디터에서 손으로 옮긴 위치·글자·색이
  화면을 다시 만들어도 유지된다 (`control_tower_layout.json`).
- 서버에 `status` / `temp_detail` 명령. 모터별 온도를 서보에 설정된
  한계값(`Max_Temperature_Limit`)과 함께 돌려준다.

### 수정

- **수동모드에서 J2/J3 가 일정 각도 이상 안 올라가던 문제.**
  원인이 두 겹이었다. 서버가 교시용 토크 한계를 500 → 320/260 으로 낮춰
  중력을 이길 힘이 모자랐고, 동시에 Unity 가 토크를 유지하는 관절의 목표까지
  실물 위치로 덮어써 슬라이더 입력이 33ms 만에 지워졌다.
- **루틴 재생이 스텝을 건너뛰던 문제.** 목표만 던지고 고정 0.5초를 기다려서,
  팔이 도착하기 전에 다음 목표가 덮어썼다. 실제 도착을 확인하고 진행한다.
- **재생 중 그리퍼가 닫히면서 팔이 출발하던 문제.** 도착 판정에서 그리퍼를
  제외했더니 아무도 기다리지 않았다. 그리퍼는 "값이 더 안 변할 때" 로 판정한다.
- **R2 단독 스텝이 (0,0,0,0,0) 자세로 재생되던 문제.** 저장은 `joints` 에,
  재생은 `joints2` 에서 읽고 있었다. 예전 형식 파일은 불러올 때 자동 변환한다.
- 서버 기동 시 팔이 스스로 움직이던 문제. `enable_torque()` 전에
  `Goal_Position` 을 현재 위치로 맞춘다.
- 속도 제한이 안 걸리던 문제. `Max_Velocity` 는 STS3215 에 없는 레지스터인데
  예외를 `try/except` 가 삼켜 로그만 성공으로 남았다. `Goal_Velocity` 로 바꿨다.
- `SOArmSimController` 배열 길이 불일치로 인한 `IndexOutOfRangeException`.
- 교시 중 그리퍼 목표가 유실되어 조작·녹화가 안 되던 문제.
- `RecordProject.cs` 주석 인코딩 복구 (CP949 → UTF-8).

### 변경

- 기존 IMGUI 화면을 관제 화면으로 대체했다.
- 그리퍼는 수동모드에서 토크를 풀지 않는다. 1:345 감속이라 손으로 벌릴 수 없고,
  풀면 녹화가 불가능해진다. 슬라이더로만 조작한다.

---

## 2026-08-01

### 추가

- 라즈베리파이 서버(`robot_server_dual.py`)를 저장소에 편입했다.
- 표준 형식 스펙 4종 (User/System Requirement, H/W·S/W Architecture).
- 아키텍처 다이어그램 (draw.io 원본 + PNG).

### 수정

- 서버 재시작할 때마다 두 팔이 떨어지던 문제. `pkill`(SIGTERM)이 종료 핸들러를
  깨워 `disable_torque()` 를 호출했다. `pkill -9` 로 핸들러를 건너뛴다.

### 변경

- 저장소에 계속 쓰는 구현만 남기고 산출물·로그·일회성 스크립트를 제외했다.

---

## v0.2 (2026-05, `v0.2-archive` 태그)

구조가 다른 초기 버전. `unity/Assets/Scripts/`, `urdf/` 배치를 썼다.
현재 `main` 과 공통 조상이 없으므로 머지하지 말 것.
